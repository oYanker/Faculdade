package aula3;

import javax.swing.JOptionPane;

import aula3.classes.ContaBanco;
import aula3.classes.Pessoa;

public class App {
    public static void main(String[] args) throws Exception {

        /*
         * Pessoa pessoa = new Pessoa();
         * pessoa.setNome(JOptionPane.showInputDialog(null,
         * "Digite um nome para mudar"));
         * pessoa.setIdade(Integer.parseInt(JOptionPane.showInputDialog(null,
         * "Digite uma idade para mudar")));
         * pessoa.setAltura(Double.parseDouble(JOptionPane.showInputDialog(null,
         * "Digite sua altura")));
         * System.out.println(pessoa.getAltura());
         * System.out.println(pessoa.getIdade());
         * System.out.println(pessoa.getNome());
         * 
         * 
         * ContaBanco conta = new ContaBanco();
         * conta.getNomeBanco();
         * conta.getNumeroConta();
         * conta.getSalario();
         * conta.setSalario(Integer.parseInt(JOptionPane.showInputDialog(null,
         * "Deseja depositar quanto?")));
         */

        Pessoa pessoa = new Pessoa();

    }
}